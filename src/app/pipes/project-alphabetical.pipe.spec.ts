import { ProjectAlphabeticalPipe } from './project-alphabetical.pipe';

describe('ProjectAlphabeticalPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectAlphabeticalPipe();
    expect(pipe).toBeTruthy();
  });
});
