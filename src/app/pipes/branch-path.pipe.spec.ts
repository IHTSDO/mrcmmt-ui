import { BranchPathPipe } from './branch-path.pipe';

describe('BranchPathPipe', () => {
  it('create an instance', () => {
    const pipe = new BranchPathPipe();
    expect(pipe).toBeTruthy();
  });
});
