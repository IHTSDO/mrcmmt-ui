import { DuplicateFilterPipe } from './duplicate-filter.pipe';

describe('DuplicateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DuplicateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
