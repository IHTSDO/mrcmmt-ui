import { DomainMatchPipe } from './domain-match.pipe';

describe('DomainMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new DomainMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
