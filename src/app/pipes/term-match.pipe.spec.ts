import { TermMatchPipe } from './term-match.pipe';

describe('TermMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new TermMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
