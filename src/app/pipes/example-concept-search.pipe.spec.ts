import { ExampleConceptSearchPipe } from './example-concept-search.pipe';

describe('ExampleConceptSearchPipe', () => {
    it('create an instance', () => {
        const pipe = new ExampleConceptSearchPipe();
        expect(pipe).toBeTruthy();
    });
});
