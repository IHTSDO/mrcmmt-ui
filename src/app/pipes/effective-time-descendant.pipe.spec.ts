import { EffectiveTimeDescendantPipe } from './effective-time-descendant.pipe';

describe('EffectiveTimeDescendantPipe', () => {
  it('create an instance', () => {
    const pipe = new EffectiveTimeDescendantPipe();
    expect(pipe).toBeTruthy();
  });
});
