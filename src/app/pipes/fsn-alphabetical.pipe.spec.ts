import { FsnAlphabeticalPipe } from './fsn-alphabetical.pipe';

describe('FsnAlphabeticalPipe', () => {
  it('create an instance', () => {
    const pipe = new FsnAlphabeticalPipe();
    expect(pipe).toBeTruthy();
  });
});
