import { ManagedServicePipe } from './managed-service.pipe';

describe('ManagedServicePipe', () => {
  it('create an instance', () => {
    const pipe = new ManagedServicePipe();
    expect(pipe).toBeTruthy();
  });
});
