import { InheritedDomainMatchPipe } from './inherited-domain-match.pipe';

describe('InheritedDomainMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new InheritedDomainMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
