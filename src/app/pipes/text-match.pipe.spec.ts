import { TextMatchPipe } from './text-match.pipe';

describe('TextMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new TextMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
